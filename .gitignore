# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Tooling caches
.cache/
.matplotlib/

# Secrets — never commit the Benchmarkoor token
secrets.json

# Large regenerable fetch artifacts (recreate with `make fetch`).
# data/raw/meta.json (build footer) and opcounts.json are kept; the bulk is not.
data/raw/runtimes.csv
data/raw/bench_data.parquet
data/raw/trace.parquet

# Proposal figs are rendered as HTML tables now, not images. evm-gasfit still
# emits these PNGs on every fit, but they're unused — keep them out of the repo.
data/gasfit/figs/proposal/
